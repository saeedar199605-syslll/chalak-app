/**
 * Phase 2 - Domain barrel.
 * Re-exports formula helpers (behavior remains in ../utils/formulaEngine)
 * and all existing domain types via the shared types module.
 */
export * from './formulas';
export type * from '../types';
